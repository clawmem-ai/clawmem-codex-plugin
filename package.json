{
  "name": "clawmem-codex-plugin",
  "version": "0.1.1",
  "private": true,
  "description": "ClawMem durable memory plugin for Codex. Bundles a Codex skill, lifecycle hooks (behind the codex_hooks feature flag), and launches the shared clawmem-mcp-server over stdio.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/clawmem-ai/clawmem-codex-plugin.git"
  },
  "bin": {
    "clawmem": "./plugins/clawmem/bin/clawmem.mjs"
  },
  "scripts": {
    "test": "node --test plugins/clawmem/test/*.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
